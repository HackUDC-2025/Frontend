import { ProfileConfig } from "./profile.interface";

export const fineartsProfile:ProfileConfig = {
    name: "finearts",
    technicalLevel : "medio",
    languageStyle: 'detallado',
    maxTokens : 200,
    specialInstructions: "Este perfil es para proyectos de arte, por lo que se espera que el texto generado sea lo más descriptivo posible.",
}