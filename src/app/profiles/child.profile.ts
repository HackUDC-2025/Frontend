import { ProfileConfig } from "./profile.interface";

export const childProfile:ProfileConfig = {
    name: "child",
    technicalLevel: "bajo",
    languageStyle: 'sencillo',
    maxTokens: 200,
    specialInstructions: "Este perfil es para el nivel de un niño, por lo que se espera que el texto generado sea sencillo y claro.",
};